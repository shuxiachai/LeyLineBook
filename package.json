{
  "name": "leylinebook-dev-tools",
  "private": true,
  "description": "开发期工具依赖，仅用于验证 static/local-backend.js（移动端 PWA 业务逻辑）的单元测试；桌面端与移动端运行时均不依赖任何 npm 包。",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5"
  }
}
