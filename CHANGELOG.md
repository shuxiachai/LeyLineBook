# 更新日志

## v3.0.5 - 2026-09-10

### 数据安全与一致性

- 桌面 API 增加启动会话鉴权；实例识别采用挑战响应，避免误关闭占用同一端口的其他服务。
- 修复快速切换账号时凭据请求乱序导致的串显和误保存；关闭弹窗后清除凭据字段。
- 修复 SQLite 脚本隐式提交破坏事务的问题，表结构迁移失败时完整回滚，并在迁移前保留数据库备份。
- PWA 的读取、校验和写入纳入同一 IndexedDB 事务，等待事务真正提交后才返回成功，避免跨标签页并发覆盖。
- 导入增加日期、范围、枚举、重复记录及关联校验；导入前快照与数据替换保持一致。
- 备份格式升级为 schema 3，保存版本周期基准日，继续支持导入 schema 2 和无版本号旧备份。

### 任务与界面修复

- 修复托管方案 UUID 被转换成数字而导致套用失败的问题。
- 修复日期切换尚未加载完成时，旧任务卡片可能写入新日期的问题；手动查看历史日期时不再被定时刷新切回今天。
- 周期任务只允许从最新记录开始依次撤销，阻止旧记录覆盖后续冷却；有较新记录时禁止向前补记。
- 探索派遣支持同一游戏日内多轮完成，以实际收取时间区分轮次并防止重复提交。
- 阻止剧情任务重复提交，并区分保存成功后刷新失败与保存失败。

### 更新与工程验证

- 自动更新等待前端初始化成功后才确认新版本可用，并阻止并发启动重复更新。
- Service Worker 仅清理本项目对应作用域及已知旧版缓存，保留其他项目缓存。
- 补充双端业务回归、真实浏览器流程和发布脚本失败传播测试；CI、Release 和 PWA 部署加入浏览器回归检查。

### 升级注意

- 升级前请保留一份旧版本导出的 JSON 备份，并先关闭正在运行的旧版程序。
- **v3.0.4 及更早版本不能导入 schema 3 备份。** 桌面与 PWA 交换新备份时，两端都需要升级到支持该格式的版本；仅替换本地 EXE 不会更新线上 PWA。
- 请通过 EXE 或源码启动器打开应用；直接输入本地端口地址不能建立新的授权会话。
- JSON 备份仍不包含账号凭据。Windows 凭据继续使用当前用户的 DPAPI 加密，PWA 不保存凭据。
- 此次更新未引入 Windows 代码签名，版本号和文件名本身不能解决系统对未签名应用的拦截。
