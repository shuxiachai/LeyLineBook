# LeyLineBook v3.0.6

本次更新重点修复精确冷却的跨时区一致性，并加强桌面升级恢复和 PWA 离线换版的可靠性。

## 时间与任务

- 质变仪和探索派遣按 UTC 时刻计算实际经过的 168 / 15 / 20 小时，修正夏令时切换前后桌面与 PWA 冷却时间相差一小时的问题。
- 精确任务的完成顺序和撤销依据真实时刻判断，避免跨时区导致业务日期先后与实际时间先后不一致。
- 补记历史精确任务必须填写实际使用时间；拒绝空白、无效时间及不完整的完成记录，避免错误修改冷却。
- 修正月度预设任务首次启用时的当前周期处理。每日、每周、每月及版本周期仍保留原业务日期规则。

## 备份与旧数据

- 备份格式更新为 `schemaVersion: 4`，精确时间契约为 `timeContractVersion: 2`。
- 旧备份中的无偏移精确时间需要确认来源时区；重复小时需明确选择，不存在的当地时间需要校准，并保留原值及确认记录。
- 桌面端校验 IANA 时区、当地时间与偏移是否一致；Windows 打包程序包含所需时区数据。
- 旧数据库中的无偏移时间不会在启动时被猜测并批量改写。

## 桌面与 PWA 可靠性

- 补齐新增号主时异步凭据保存的生命周期处理，避免后续打开的弹窗影响此前保存。
- 桌面更新保留完整 SQLite 快照、旧程序及恢复说明；新版本首次初始化旧库前也会准备恢复材料，覆盖由旧更新器发起的首次升级。
- 初始化和内部恢复检查点保持同一事务；异常回滚日志会被保留并阻止继续初始化，不自动删除日志或覆盖数据库。
- 修正 Windows 合法短文件名路径被误判为重定向的问题；数据库恢复仍拒绝符号链接、junction 和硬链接等不安全路径。
- PWA 使用完整版本缓存与等待式换版，允许不完整缓存重试，避免新旧资源混用；脚本加载失败不会以 HTML 页面代替。
- 补充业务、离线生命周期、恢复和冻结 EXE 回归，并将打包时区与认证 API 检查接入 CI / Release 门禁。

## 升级前请注意

- **v3.0.5 及更早版本不能导入 schema 4 备份。** 桌面与 PWA 交换新版备份前，需确认两端均已更新；更新本地 EXE 不会自动更新设备上的 PWA 缓存。
- 先退出所有应用实例，并保留原 EXE 和完整数据目录（包括可能存在的 WAL / SHM / journal 文件）。普通 JSON 备份不含账号凭据，不能代替完整数据库备份。
- 自动首次升级保护核验原始命名的官方 `LeyLineBook-v3.0.5-Windows-x64.exe`。更早版本或旧 EXE 已改名的用户，应保留完整备份后按手动方式升级；不要通过删除数据库或回滚日志解决启动失败。
- 恢复仅在另一全新数据目录进行。首次保护保存的旧程序为 `.exe.saved`，需复制到独立目录后再恢复 EXE 扩展名，并显式设置 `LEYLINEBOOK_DATA_DIR`；不要让旧程序直接打开当前数据库。
- 完整快照包含加密凭据，不应公开上传；解密仍受原 Windows 用户和设备限制。
- 本版本仍未增加 Windows 代码签名，系统可能拦截未签名程序。版本号和 SHA-256 校验不等于代码签名，也不保证系统放行。
