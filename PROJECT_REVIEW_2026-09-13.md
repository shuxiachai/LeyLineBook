# LeyLineBook 项目复核记录

> 本文保留第一阶段验收结果及当时的版本状态。后续 3.0.6 本地候选、首次升级保护和新增验收见 [3.0.6 本地候选验收](RELEASE_READINESS_3.0.6.md)；后续结果不抹去本文记录的历史中断或验收限制。

## 范围与基线

- 基线提交：`c619e2f`，已发布版本：v3.0.5。
- 本次为本地未发布修改；不操作真实数据库、用户凭据、既有正式 EXE 或线上 PWA。仅在隔离目录构建和运行测试 EXE。
- 工作顺序：基线问题定位、限定范围实现、隔离回归、独立只读复核。
- 当前记录随验收更新，不把代码检查、模拟测试、真实进程测试和原生窗口验收混为一谈。

## 模块与数据流

项目面向原神多账号玩家和代肝工作室，是本地任务与记录管理工具，不连接或操作游戏。桌面与 PWA 共用原生 HTML/CSS/JavaScript 界面；桌面通过 Python 标准库 HTTP 服务和 SQLite 持久化，PWA 通过兼容接口与 IndexedDB 持久化。两端没有云端自动同步，依靠 JSON 备份交换数据。

桌面窗口使用 pywebview/WebView2，发行包由 PyInstaller 构建；Windows 凭据依靠 DPAPI 加密，普通导出备份不含凭据。PWA 通过 manifest 和 Service Worker 支持安装与离线。开发验证使用 Python unittest、Node.js 内置测试器、fake-indexeddb、Playwright，以及 Windows 原生进程测试夹具；npm 依赖只用于开发测试。CI、Release 与 GitHub Pages 部署各自承担验证、发行和静态站点部署，不存在生产 Node.js 服务。

来源时区校验使用 Python 标准库 zoneinfo 与浏览器 Intl。为使冻结 EXE 不依赖本机是否安装 IANA 数据库，PyInstaller 显式收集 tzdata 数据；该包属于运行所需的时区数据，不应再描述为仅测试依赖。

| 模块 | 职责 | 核对重点 |
| --- | --- | --- |
| VBS/BAT/PowerShell 启动器 | 定位 Python 并启动应用 | 不重复发送未经认证的关闭请求 |
| app.py | 本地 HTTP API、SQLite 业务、DPAPI 凭据与窗口启动 | 会话鉴权、事务、日期与瞬间边界 |
| static/app.js 与 index.html | 桌面/PWA 共用界面 | 请求生命周期、输入快照、日期加载和备份确认 |
| static/local-backend.js | PWA API 与 IndexedDB | 操作级事务、跨标签页写入、双端契约一致 |
| static/time-contract.js | 精确时刻与旧时间确认 | UTC 计算、夏令时歧义、保留原始信息 |
| static/sw.js | PWA 应用外壳缓存 | 离线重开、完整换版、不把脚本请求回退成 HTML |
| update_recovery.py | 升级前快照与恢复证据 | 完整性检查、非交互等待、不覆盖当前数据 |
| tests 与 GitHub Actions | 单元、契约、浏览器和发布验证 | 独立数据目录、失败传播、真实验证范围 |

## 本轮调整

- 时间统一：新精确冷却以 UTC 瞬间计算，前端显示本地时间；旧记录继续标识为 legacy-local，不在启动时猜测迁移。
- 备份兼容：schema 4/timeContractVersion 2；旧备份确认来源时区、重复小时及校准值，并保留确认档案。已发布的 v3.0.5 无法读取新格式。
- 业务边界：补记必须提供实际精确时间；拒绝显式空完成时间；预设月度任务首次启用指向当前已开启周期；同秒历史排序保留数值 ID 语义。
- 界面状态：新增号主提交时固定输入和目标，延迟请求不能使用后来打开弹窗中的凭据，也不能关闭新弹窗。
- 启动安全：VBS 不再自行探测并发送未经认证的关闭请求，交由现有 Python 身份确认路径处理。
- 更新恢复：替换前保留完整 SQLite 快照并检查完整性，保留旧程序、当前数据库与失败原因；不自动覆盖数据库，也不自动让旧程序打开可能已变更的数据。
- 离线换版：缓存完整应用外壳，旧页面全部退出后再启用新版本；资源下载及时消费响应内容，避免安装过程因连接占用而挂起；脚本失败不返回 HTML。
- 验证接线：补充账号/时间、Service Worker、Windows 升级恢复和原生窗口测试；CI/Release 保留失败即停止规则。

## 必须保持的不变量

- 168/15/20 小时表示实际经过秒数；业务日期仍按当前规则计算，不能用 UTC 日期替代。
- 精确任务的最新记录、完成和撤销顺序按真实时刻判断，不能因跨时区后的业务日期变小而误判向前补记。
- 旧无偏移记录没有足够信息恢复唯一时刻，不能统一加减一小时或直接追加 Z。
- 备份导入先完整校验，失败不替换原数据；撤销必须保留对应的时间语义。
- 凭据只能写给提交时固定的号主，旧请求不能读取或关闭后来打开的弹窗。
- 历史精确冷却不得使用当前时刻冒充实际使用时刻。
- 时间输入先规范化再检查必填；空白字符串不能绕过历史实际时间要求。IANA 来源确认必须与 UTC 时刻、当地时间和偏移往返一致，不能只在界面校验。
- 更新失败保留程序、当前数据库、升级前快照和原因；保留旧 EXE 不等于自动数据库回滚。
- 测试只用隔离数据；完整数据库快照含凭据，不进入 Git。

## 验收记录

以下在 Windows PowerShell 环境使用隔离数据库、测试凭据与浏览器 profile 执行，不使用真实用户数据。Python、JavaScript 和常规浏览器全量由未参与实现的测试角色重新运行；PWA 专用模式在纠正测试命令后由专家补测，升级中断由专家定向复验。结果不合并成“全部测试无异常”。

| 验证 | 命令 | 结果 |
| --- | --- | --- |
| Python 全量 | `python -B -X utf8 -m unittest discover -s tests -v` | 93 通过，0 失败，0 跳过 |
| PWA 与时间契约 | `npm test` | 55 通过，0 失败，0 跳过 |
| 浏览器全量 | 设置 `PLAYWRIGHT_CHANNEL=msedge` 后运行 `npm run test:browser` | 15 通过，0 失败，0 跳过 |
| Windows 升级协调 | `npm run test:upgrade` | 最新整轮前 5 场景符合预期，第 6 场景中断；不改代码的 late 定向复验通过 |
| 真实源码原生窗口 | `python -B -X utf8 scripts/windows_ui_smoke.py` | 1 通过，实际 WebView2 与前端就绪握手 |
| 真实冻结 EXE 时区 API | `python -B -X utf8 scripts/frozen_time_smoke.py --exe <隔离构建路径>` | 5 场景通过，身份挑战与实际打包 tzdata 资源检查通过，进程正常退出 0 |
| PWA 部署专用模式 | 同时设置 `PLAYWRIGHT_CHANNEL=msedge`、`LEYLINEBOOK_PWA_ONLY=1` 后运行 `npm run test:browser` | 11 通过，1 显式跳过桌面凭据场景，0 失败 |
| 桌面/PWA 接口定向 | 清除 `LEYLINEBOOK_PWA_ONLY` 后运行 `node --test tests/account_time.e2e.js` | 6 通过，0 失败，0 跳过，正常桌面覆盖保留 |
| 静态检查 | Python AST、所有 static/tests JavaScript（含新增文件）语法与 `git diff --check` | 通过，无空白错误 |

升级测试覆盖成功、移动失败、无法启动、启动后退出、健康检查超时、超时后才就绪；故障场景应返回失败标记，因此夹具验证成功不表示更新成功。早期完整通过报告为 `output/playwright/upgrade-fixture-pd7fubar/report.json`；最终全量运行的 `output/playwright/upgrade-fixture-qt64pgnl` 在 late 场景返回 `0xC000013A`，日志出现中断处理提示，不能据此归因用户或工具。生产和夹具均使用 DETACHED_PROCESS，未发现启动标志不一致；构建时间晚于异常，可排除本轮冻结构建并发。中断来源仍未确定。

在不修改生产代码、夹具或断言的情况下，仅对 late 定向复验通过，报告为 `output/playwright/upgrade-late-diagnostic-pyzxeo9d/report.json`：32.954 秒返回预期健康超时，旧 EXE、完整快照及测试凭据保留，迟到的就绪信号不改判成功、不覆盖后续数据。保留原整轮失败记录，不能把单项复验写成重新完整通过六场景。最新源码原生窗口报告为 `output/playwright/native-ui-484phwdp/report.json`。报告和隔离数据不纳入源码提交。

PWA 验证曾因测试命令误设 `PWA_ONLY` 而实际仍跑桌面分支；该次结果不计为 PWA 专用证据。正确变量是 `LEYLINEBOOK_PWA_ONLY`，补测先打印 Node 进程收到的变量值，再验证 11 通过、1 预期跳过。

冻结 EXE 仅构建一次，使用当前源码、PyInstaller 6.21.0、pywebview 6.2.1 与 tzdata 2026.2，APP_VERSION 仍为 3.0.5。最终报告为 `output/playwright/frozen-time-c5fxla8o/report.json`：夏令时空缺、无效时区、错配偏移均拒绝且数据不变；重复小时的两个合法选择均通过导入、导出、归档和状态语义校验。运行使用独立 TEMP/DATA_DIR、端口和会话，空 PYTHONTZPATH，实际检查了解包后的 Sydney TZif 资源；不是借用源码解释器执行 API，也没有伪造就绪信号。

冻结脚本前三次验收分别因日志位置、Windows 编码、固定导入任务 ID 的错误假设失败，报告完整保留；这些不是生产时区校验失败。修正脚本并经过另一审查者预审后，同一 EXE 第四次运行通过全部五项。冻结脚本目前为可手动复用的验收命令，尚未加入 CI/Release；没有据此宣称远端门禁执行过它。

桌面 1280px 和手机 390px 的旧时间确认弹窗、PWA 断网重开截图已人工核对，关键输入与按钮可见，没有发现本轮新增弹窗的重叠或截断。真实浏览器验证包含持久 profile 重启、离线重新打开、双旧页面退出后激活新版本、残缺应用外壳拒绝安装和脚本失败不返回 HTML。

CI/Release 的 Python 编译和 JavaScript 语法检查已纳入新增生产模块，Pages 同样检查 `time-contract.js`。现有 PowerShell 失败门禁回归通过；Pages 六条验证命令在本地 Git Bash `-e` 下分别注入失败后均立即停止。PWA 专用模式只在本机 Windows/Edge 验证，未据此宣称远端 Ubuntu 验证已通过。

独立只读审查已覆盖稳定生产差异和全项目关键链路；发现的问题均已完成源码级修复并定点复审关闭，包括后续发现的缓存中断重试、跨时区精确顺序、空白时间绕过和来源时区校验不一致。冻结脚本另经独立执行前审查，实际运行结果单列；源码问题关闭不等同于所有环境已验证。

## 发布前边界

- APP_VERSION 暂保持 3.0.5；正式发布前必须提升版本并同步两端，现有 EXE 不包含本轮源码改动。
- PWA 应用外壳资源变化时，必须同步提升 SW 缓存版本并验证新旧页面换版；仅修改 APP_VERSION 不会使已安装页面自动丢弃完整旧缓存。
- schema 4 不能交给已发布的 v3.0.5 及更早版本导入。
- 游戏日究竟采用设备时区还是服务器时区属于独立业务决策，本轮保留原规则。
- Windows 代码签名和系统安全策略未调整；不能以自动化测试通过保证所有设备均允许运行。
- 当前验证没有执行完整的已发布旧 EXE 到新打包 EXE 升级，也没有运行远端 GitHub Actions；Windows 真实合成进程测试和源码原生窗口测试不能替代该链路。
- 一次升级测试中断来源仍未确定，定向复验通过不抹去原失败；正式发布前应在稳定 Windows 环境补做完整旧版到新版升级与失败恢复验收。
- 回退旧程序前应保全当前数据，在隔离目录验证对应版本快照；不能假定旧程序可直接理解新时间契约写入的当前数据库。
- 旧数据库中的精确时间及旧树脂备注缺少可信来源时区，仍保留旧解释；用户确认旧备份时转换并归档，不自动按旧使用时间重算历史期限。
- 完整升级快照含加密凭据，长期保留会占用磁盘；应在确认新版本稳定且另有备份后，由用户自行管理，不应自动清理尚可能用于恢复的材料。
- `app.py` 和前端业务模块仍较集中，本轮只收敛已确认问题。以后拆分应围绕现有双端契约测试分阶段进行，不宜把当前修复扩大为框架重写。
