# LeyLineBook / 地脉簿

《原神》（Genshin Impact）多账号任务管理工具，面向同时打理多个账号的玩家和代肝工作室。集中追踪体力、深境螺旋、幻想真境剧诗、危战、剧情任务、限时活动等日常与周期内容，支持桌面端（Windows）与[手机端（PWA）](https://shuxiachai.github.io/LeyLineBook/)。纯本地记录工具，不会连接或操作游戏本体，所有数据仅保存在本机。

## 启动

双击 EXE 后程序会打开独立的应用窗口（基于系统 WebView2 内核），关闭窗口即退出程序。再次双击会重启并打开新窗口。

GitHub Release 提供单文件版 `LeyLineBook-v版本号-Windows-x64.exe`，无需安装 Python，也不依赖 `_internal` 文件夹。首次启动时需要解压内置运行资源，因此可能比后续启动稍慢。打包版可在“设置与备份”中检查 GitHub Releases 新版本。

版本变更与升级注意事项见 [更新日志](CHANGELOG.md)。

**当前工作区为尚未发布的 3.0.6 本地候选，现有 v3.0.5 EXE 和线上 PWA 不会因本地源码修改而自动更新。** 当前验收范围与结果见 [3.0.6 本地候选验收](RELEASE_READINESS_3.0.6.md)，前一阶段结果见 [项目复核记录](PROJECT_REVIEW_2026-09-13.md)。

若系统缺少 WebView2 运行时（极少见），程序会自动回退到浏览器模式；也可以用 `--browser` 参数强制使用浏览器打开。浏览器模式下关闭所有页面后，后台程序会在约 80 秒内自动退出；任一模式下都可以进入“设置与备份”，点击“关闭程序”立即停止。

单文件 EXE 的数据保存在 `%APPDATA%\LeyLineBook\task_records.db`，日志保存在同一目录。首次升级时，如果 EXE 旁存在旧数据库且新目录尚无数据库，程序会自动复制过去。删除数据库后，程序只会创建空数据库，不会读取 Excel 或生成内置名单。本程序只支持自身 JSON 备份导入，不提供 Excel 导入功能。

## 手机版

浏览器打开 **[https://shuxiachai.github.io/LeyLineBook/](https://shuxiachai.github.io/LeyLineBook/)**，菜单选择"添加到主屏幕"即可安装，支持离线使用。手机与电脑数据各自独立保存，可通过导出/导入备份手动同步。手机版不会保存账号、密码或验证码；导入前会自动创建本地快照，最近保留 5 个并可在设置页恢复。

## 主要功能

- 今日任务：单个完成、按号主一键完成、全部标记完成、撤销、切换日期补记
- 周期任务：深境螺旋、幻想真境剧诗、危战会按各自刷新周期显示
- 限时活动：大活动/小活动按时长自动计算截止时间，可一键为全部号主启用
- 剧情任务：按号主记录魔神任务、传说任务和世界任务，可标记额外奖励期限
- 号主管理：新增、修改、停用号主，按住右侧拖动把手可调整顺序
- 托管方案：自定义任务组合模板（如普托/精托），可含大/小活动开关，创建号主时一键套用，创建后与方案互不影响
- 凭据管理（Windows）：可选保存账号名称、密码和凭据备注，使用当前 Windows 用户的 DPAPI 加密；PWA 不提供凭据存储
- 任务管理：每日、间隔周期、一次性任务；体力任务可用快捷标签记录周本（支持自定义周本名称），并可选记录当前树脂、显示预计回满时间；壶冷却期间可提前收取并顺延冷却
- 历史记录：按日期范围查询，支持按号主和任务筛选
- 数据备份：采用带格式标识和版本号的 JSON 备份，兼容旧版文件；桌面端与 PWA 导入前都会保存快照
- 版本更新：启动时静默检查新版本，自动更新仅接受精确命名的 GitHub Release 资产，并校验 SHA-256、下载大小和新版本启动状态
- 界面主题：黑、白、绿、蓝、紫、玫瑰、琥珀七种主色调，选择后自动记忆
- 角色主题：从本机选择图片后自动提取主色与明暗，启用全屏背景和磨砂界面；点击任一纯色主题即可恢复纯色，图片只保存在本机的应用窗口存储中

## 固定刷新规则

- 幻想真境剧诗：每月 1 日刷新
- 深境螺旋（深渊）：每月 16 日刷新
- 危战：版本更新后第 8 天（第二周星期三）10:00 开放，版本最后一周星期二 03:59 关闭

危战以官方 Luna VII 更新日 `2026-05-20` 为锚点，按原神常规42天大版本周期自动滚动。危战在版本第8天10:00开放，并在下一版本更新前一天03:59截止。只有官方发生延期或改期时，才需要在“设置与备份”中校准版本日期。

## 每日任务分类

- 体力：备注可标注“好感队”“委托”“木偶食材”，也支持自定义备注
- 狗粮：作为独立的每日任务记录，次日卡片显示前一天的采集时间（采集点 24 小时刷新）

“号主与任务”页面分为两组标签：

- 每日：体力、狗粮、质变仪（168小时）、壶（3天）、爱可菲料理（每周一 04:00 刷新）、探索派遣（15或20小时），体力备注包含好感队、委托、周本和自定义备注
- 深渊：深境螺旋、剧诗、危战，点击标签直接启用或停用

点击任务标签即可启用或停用；捡材料已从任务标签中移除，旧历史记录仍保留。

“爱可菲料理”每周任意时间完成一次即可，完成状态保持到下周一 04:00，并计入每日进度。

探索派遣按实际收取时间记录轮次，支持同一游戏日内多轮 15/20 小时派遣。重复提交同一收取时间不会重复计数。间隔、每月和版本周期任务需要从最新记录开始依次撤销，避免旧记录覆盖后续冷却；存在较新完成记录时不支持向前补记周期任务。当前未发布源码中，精确冷却任务按真实完成时刻判断先后，不用业务日期先后替代时间顺序；跨时区后记录的业务日期仍保留原归属。

## 剧情任务

- 魔神任务：可选额外奖励，截止到当前版本末周二 15:00
- 传说任务：可选额外奖励，截止到当前半版本末周二 15:00
- 世界任务：默认无额外奖励期限

奖励期限结束后任务不会消失，仍会保留在待完成列表中。
剧情任务的号主既可以从现有名单选择，也可以直接输入临时名称；任务名称留空时会自动使用任务类型名称。

## 数据安全

所有数据都在本机。Windows 版账号凭据使用 DPAPI 加密，仅能由保存凭据时的 Windows 用户账户解密；PWA 不存储凭据，并会在数据库升级时清除早期版本可能留下的明文字段。导出的 JSON 备份不包含凭据，导入后需要重新填写。请勿在普通备注中填写密码或验证码，也不要把真实数据库上传到 GitHub。建议定期导出 JSON 备份；数据库中的加密凭据换电脑或更换 Windows 用户后无法解密。

从 v3.0.5 起，桌面 API 使用启动会话凭证，凭证通过启动链接的 fragment 交付，页面接收后会移除该 fragment；服务端不提供公开取回凭证的接口。实例识别使用挑战响应，Windows 临时目录中的实例凭证由 DPAPI 加密。请通过启动器或 `python app.py --browser` 打开应用，单独输入本地端口地址不能建立新会话。这些措施不用于抵御已经控制当前 Windows 用户会话的恶意程序。

已发布的 v3.0.5 使用 `schemaVersion: 3` 备份，包含版本周期基准日和派遣轮次信息，仍可导入格式 2 及无版本号的旧备份。**v3.0.4 及更早发行版不能导入格式 3；跨端交换新备份时，两端都须使用支持格式 3 的代码版本。** 旧备份缺少版本基准日时采用默认基准日。导入先检查业务字段与关联，再在事务中替换；重建旧数据库表前会保留 `*-pre-migration-*.db` 备份。当前未发布源码的格式 4 规则见下一节。

### 3.0.6 候选的时间与恢复改进

当前源码的备份格式为 `schemaVersion: 4`，精确时间契约为 `timeContractVersion: 2`。质变仪和探索派遣的新记录按 UTC 时刻计算实际经过的 168/15/20 小时，展示时再转为本地时间。每日、每周、每月与版本日期仍按现有业务日期规则处理，没有改成 UTC 日期。

旧数据库中的无偏移时间不会在启动时被批量改写。旧备份导入需要确认精确冷却时间的来源时区；遇到重复小时需选择具体时刻，不存在的本地时间需校准。确认过程保留原始值和选择记录。**已发布的 v3.0.5 及更早版本不能导入格式 4；跨端交换前须确认两端支持相同格式。** 历史日期补记精确冷却任务必须填写实际使用时间，不能在批量完成时默用当前时间。

候选版自身发起自动更新时，在替换程序前准备完整 SQLite 快照并执行完整性检查，保存在数据目录的 `update-recovery/` 下，同时在 EXE 旁留下恢复说明。升级成功或失败均保留恢复材料，不自动覆盖当前数据库，也不自动让旧程序打开可能已变化的数据。

从已发布的旧更新器首次进入候选版时，另有启动前保护：在写入已有数据库之前保留完整快照和恢复说明，自动首升还需保存并核验同目录原始命名的官方 v3.0.5 EXE。初始化和内部检查点在同一事务提交，准备失败不报告就绪；检查点不等于界面已启动。完成后正常启动不再依赖历史恢复文件，换入没有检查点的旧库则重新保护。全新数据库不伪造旧快照；自动首升即使没有数据库也先保全旧程序。

**自动首升校验目前只覆盖 `LeyLineBook-v3.0.5-Windows-x64.exe` 及其官方 SHA-256。** 更早版本或改名旧 EXE 不会被扫描猜测，应先退出旧程序并保留原 EXE 与完整数据目录，再手动升级。手动替换且旧 EXE 已不存在时，恢复清单会明确写明需重新下载，并不代表已保存旧程序；v3.0.5 下载地址不能证明更早来源或已被新版写入的数据与旧版兼容。

完整快照包含加密凭据，不能公开上传。恢复时先退出所有应用进程并保留当前完整数据目录（包括可能存在的 WAL/SHM/journal 文件），只在另一全新目录检查升级前快照；首次启动保护保存的旧程序存档为 `.exe.saved`，需复制到独立目录后再恢复 EXE 扩展名，并在启动前显式设置 `LEYLINEBOOK_DATA_DIR`。不要从恢复目录直接打开旧程序或覆盖当前数据库。该隔离变量适用于 v3.0.5 起的版本，凭据解密仍受原 Windows 用户及设备限制。检测到非空回滚日志时，程序会在 SQLite 连接前停止；不要删除该日志，应保留完整原件后在隔离副本中处理。

PWA 使用按版本保存的完整应用外壳缓存。新 Service Worker 等旧页面退出后再接管，脚本资源失败时不会返回 HTML 页面。没有完成标记且资源不完整的安装残留允许重建；完整旧缓存不会因重试而被覆盖。离线首次使用仍需先在线完成缓存，设备上的记录仍保存在 IndexedDB 中。后续发布应用外壳资源变更时，必须同步提升 `static/sw.js` 的缓存版本并验证换版，不能只修改应用版本号。

## 演示数据

仓库中的 `demo/task_records.demo.db` 只包含虚构数据，可用于截图、录屏和功能展示。使用方法见 [`demo/README.md`](demo/README.md)。请勿用演示库覆盖自己的真实数据库。

## 角色主题与素材说明

角色主题支持用户从本机选择图片，图片仅保存在本机应用窗口的本地存储中（`%APPDATA%\LeyLineBook\webview`），不会写入任务数据库、备份文件或公开发行包。本仓库不包含《原神》官方角色立绘、音乐、语音或其他游戏素材。

LeyLineBook 是非官方、非商业的个人项目，与 HoYoverse 或 COGNOSPHERE 没有隶属、赞助或认可关系。《原神》及相关角色、美术和商标权利归其各自权利人所有。详见 [`NOTICE.md`](NOTICE.md)。

## 开发与打包

HTTP 服务、SQLite 与核心业务逻辑基于 Python 标准库；备份来源时区校验使用标准库 `zoneinfo`，构建时收集 `tzdata` 时区数据，避免依赖 Windows 是否提供 IANA 数据库。应用窗口依赖 `pywebview`（基于系统 WebView2 内核）。开发环境建议使用 Python 3.11 或更高版本。

```powershell
python -m pip install -r requirements-dev.txt
python app.py
python -m unittest discover -s tests -v
python -m PyInstaller --clean -y LeyLineBook.spec
python -B -X utf8 scripts/frozen_time_smoke.py --exe dist/LeyLineBook.exe
```

移动端 PWA 的业务逻辑（`static/local-backend.js`，用 IndexedDB 复刻后端接口）另有一套 Node.js 测试。两端共同读取 `tests/fixtures/scheduling_cases.json`，持续验证每周刷新、版本窗口、到期任务和汇总统计的一致性：

```powershell
npm install
npm test
npx playwright install chromium
npm run test:browser
npm run test:sw
npm run test:upgrade
python -B -X utf8 scripts/windows_ui_smoke.py
```

浏览器回归使用独立临时数据库，覆盖桌面与手机宽度下的方案套用、凭据乱序、日期切换、重复提交和备份恢复；不会读取用户数据库。也可设置 `PLAYWRIGHT_CHANNEL=msedge` 使用已安装的 Edge。业务字段的双端导入约束通过 `tests/fixtures/backup_cases.json` 验证。`LEYLINEBOOK_DATA_DIR` 可指定隔离数据目录，设置后不会自动复制旧数据库；自动化场景可通过 `LEYLINEBOOK_SESSION_TOKEN` 传入仅供该进程使用的高熵会话凭证。

`test:browser` 已包含独立账号/时间回归和 Service Worker 生命周期测试，`test:sw` 仅运行后者。`test:upgrade` 在 Windows 上使用合成 EXE 验证真实进程替换、启动失败和超时保留恢复材料。`frozen_time_smoke.py` 使用独立端口、会话和数据目录验证实际打包 EXE 的认证 API 与时区资源，不打开界面。本轮仅做无界面验收，不运行 `windows_ui_smoke.py` 的原生窗口，也不运行会打开候选界面的真实旧协调器。合成进程及无界面 API 测试均不等同于完整自动升级；具体产物与结果以本地候选验收记录为准。

`scripts/real_upgrade_smoke.py` 接收 `--new-exe` 与 `--new-sha256`，核验官方 v3.0.5 旧包后只运行隔离副本，以无界面方式检查首次保护、完整 SQLite 快照、旧程序保存、新写入保全和独立恢复。该脚本不运行真实旧协调器、不伪造界面就绪，也不覆盖完整更新发现与下载。

仓库包含三条 GitHub Actions 流水线：`CI` 在 Windows 上执行双端测试、浏览器回归、PyInstaller 打包、冻结 EXE 时区与认证 API 检查以及启动冒烟；`Release` 在相同测试通过且标签与 `APP_VERSION` 一致时发布 EXE 与同名 `.sha256` 文件，任一外部测试命令失败都会阻止打包发布；`Deploy PWA` 在 `main` 分支的静态资源变更通过其测试后部署 `static/` 到 GitHub Pages。本轮只修改本地配置，没有触发这些远端流水线。

数据库、日志、缓存、`node_modules` 和打包结果均不应提交到源码仓库，具体规则见 `.gitignore`。
