# LeyLineBook 3.0.6 发布验证进度

本记录衔接 [本地候选验收](RELEASE_READINESS_3.0.6.md)。用户现已批准按“验证分支、远端 CI、真实 Windows 界面与旧版升级、协调 Release/PWA”的顺序继续。本地候选阶段的无界面和不推送要求仅描述当时范围，不代表本阶段的授权。

## 当前状态

| 项目 | 状态 |
| --- | --- |
| 候选版本 | 3.0.6；schema 4 / timeContract 2；PWA 缓存 v7 |
| 验证分支 | 已推送 `codex/release-3.0.6-validation`；首个候选提交 `39b14aab1e56c26156153d9816e3868d43a28738` |
| 远端 CI | 首轮 [运行 34750375865](https://github.com/shuxiachai/LeyLineBook/actions/runs/34750375865) 未通过；正在修复 Windows 短路径兼容问题 |
| 真实 Windows 界面及旧协调器 | 准备隔离验收，尚未标记通过 |
| 正式 Release | 未发布；必须先通过发布门禁 |
| PWA | 未部署；不提前推送主分支触发上线 |

## 原则

- 原始正式 EXE 只读，测试仅运行隔离副本；不使用真实账号数据库或凭据。
- 真实界面验收可以短暂打开测试应用窗口，不关闭用户已有应用，不修改系统安全设置。
- 只有验证完成的产物才能进入正式发布；CI 重建的 EXE 与本地候选分别记录哈希，不混为同一个包。
- Windows 与 PWA 的 schema 4 兼容性需要协调发布，并在发布说明中明确旧版本限制。
- 默认不推送 `main` 或 `v3.0.6` 标签以试探流程；先确认门禁和发布顺序。
- 具体运行、提交、产物与部署结果在各阶段完成后补齐，不把计划写成已完成。

## 已执行操作

- 刷新远端后，`origin/main` 仍为 `c619e2f`，没有遗漏的远端提交或已有 v3.0.6 Release。
- 显式提交 38 个候选源码、测试和说明文件；没有纳入 EXE、数据库、测试输出或 `RESUME_WRITEUP.md`。
- 仅推送验证分支并手动运行 `CI`，没有推送主分支或版本标签，也没有部署 PWA、创建或发布 Release。

## 首轮 CI 发现

- Windows / Python 3.11.9 上执行 124 项桌面测试，结果为 1 failure、72 errors；PWA、浏览器、构建及产物上传均未执行。
- 主要错误为恢复路径校验将 `C:\Users\RUNNER~1\...` 的合法 Windows 8.3 短路径别名与真正路径重定向混淆；另有恢复说明断言使用短路径字符串，而实际说明已输出规范长路径。
- 原始失败日志保存在本地 `output/playwright/remote-ci-34750375865/failed-steps.log`。本问题的远端验证失败计数为 1，不能由既有本地测试通过结果抵消。
- 修复必须保留相对路径、符号链接 / junction、硬链接及数据库旁文件的安全检查；不通过修改 CI 临时目录或跳过断言掩盖问题。
- 路径修复后重新验证和打包，旧本地候选仅作为上一阶段基线，不能冒充修后正式产物。

## 后续发布顺序

### 修复后的本地回归

- 路径修复通过 Win32 长文件名查询识别合法 8.3 别名，并在解析前逐层检查 reparse point；数据库、旁文件和启动锁使用同一规范路径。
- Python 3.11 完整回归：141 项，138 通过、3 跳过，无失败。3 项为符号链接创建权限不足（Windows 1314）；真实 junction、短文件名、硬链接、WAL 和异常回滚日志场景已执行。
- JavaScript 单元测试：55 项全部通过。日志保存在 `output/playwright/release-validation-20260913T200841/`。
- 草稿门禁的首轮负向测试发现 PowerShell 数组响应可误过对象校验；作者定向测试与并行完整测试各失败一次，历史失败计数为 2。修复显式拒绝数组，保留实际布尔值和精确标签要求；修后 28 种草稿响应 / 上传结果用例全部通过，并已获独立复核。
- 上述本地结果不替代修后远端 CI、真实原生升级或最终 Release 附件验收。

### 执行顺序

1. 在验证分支提交经复核的路径修复和草稿发布门禁，针对新提交重新运行完整 CI。
2. 下载成功 CI 的实际 EXE，记录哈希，并用隔离的官方 v3.0.5 副本验收真实 Windows 界面及原旧版升级协调器。
3. 创建指向已验证提交的 v3.0.6 草稿，使用详细版本说明；创建前后核验远端标签及草稿状态，不假设创建草稿一定不会建立标签。
4. 标签触发的 Release 工作流仅向同标签的已有草稿上传；缺少草稿、查询失败或已经公开时应停止，不自动公开发布。
5. Release 重新构建的附件需要独立下载、核对校验和并验收；附件有任何替换即重新验证，不能复用另一份 EXE 的结果。
6. 验收通过后公开 Release，再验证正式更新发现和下载，最后合入主分支部署并检查 PWA。发生阻塞则停在该门禁前，不回退数据库格式来强行上线。

草稿、标签与发布参数参照 [GitHub CLI 官方说明](https://cli.github.com/manual/gh_release_create) 和 [Releases API](https://docs.github.com/en/rest/releases/releases#create-a-release)；实际操作以回读的提交、标签、草稿及附件状态为准。
